# SQLite by default so the app runs with zero database setup.
DATABASE_URL="file:./dev.db"

# To use Postgres instead, set the URL below and change the datasource
# provider in prisma/schema.prisma from "sqlite" to "postgresql".
# DATABASE_URL="postgresql://user:pass@localhost:5432/fantasy_copilot"
