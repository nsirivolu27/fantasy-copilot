name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: "file:./ci.db"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate

      - name: Unit tests (no network, no database)
        run: npm test

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Create schema
        run: npm run db:push

      - name: Build
        run: npm run build
